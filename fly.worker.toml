app = "tinker-worker"
primary_region = "ord"

[build]
  dockerfile = "apps/worker/Dockerfile"

[env]
  NODE_ENV = "production"

# Workers have no HTTP service — they consume from BullMQ queues
[processes]
  worker = "node dist/worker.js"

[[vm]]
  memory = "1gb"
  cpu_kind = "shared"
  cpus = 1
